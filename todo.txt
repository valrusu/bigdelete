- fail nicer if the delete fails (maybe due to a FK constraint); currently it just crashes
- add tnsadmin parameter
    - full path
    or 
    - same directory as the executable with "."
    - see dataarchive
    - make dataarchive pass it to bigdelete
- option to group rowids so a batch of N gets deleted by the same goroutine
